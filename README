тут используется django, поэтому чтобы запусть все это необходимо выполнить ряд несложный действий..
1. в settings.py прописать путь к базе. (создать фейковую базу мускула)
2. установить django-http-proxy (служит для проксей для кросдоменных запросов) http://httpproxy.yvandermeer.net/ 
	в убунту это можно сделать так
	sudo apt-get install python-setuptools
	sudo easy_install  django-http-proxy
3. в корне проекта выполнить python manage.py syncdb
4. запустить тестовый сервер python manage.py runserver
теперь - localhost:8000/ - корневая страница где собственно сам проект
localhost:8000/test/ - страница которая отправляет тестовые данные на сервер

